This directory contains everything in the mudlib that needs any sort
of special security clearence in order to work properly.

Copy user.c into user_bak.c once in a while if you're sure it's not error prone.
