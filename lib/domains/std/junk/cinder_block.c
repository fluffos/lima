/* Do not remove the headers from this file! see /USAGE for more info. */

inherit JUNK;

void setup()
{
   set_id("cinder block", "block");
   set_direct_salvage((["concrete scrap":2]));
   set_long("A molded concrete block in rectangle shapes used for construction typically.");
   set_weight(10);
}