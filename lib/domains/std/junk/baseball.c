/* Do not remove the headers from this file! see /USAGE for more info. */

inherit JUNK;

void setup()
{
    set_id("baseball", "ball");
    set_direct_salvage((["cork scrap":2, "raw leather":1]));
    set_value(4);
    set_long("A baseball is a ball used primarily in the sport of the same name. "
             "The ball features a rubber or cork center, wrapped in yarn and covered leather.");
    set_weight(1);
}