/* Do not remove the headers from this file! see /USAGE for more info. */

inherit JUNK;

void setup()
{
   set_id("mop");
   add_adj("old", "useless");
   set_direct_salvage((["wood scrap":2]));
   set_long("This is old useless mop can be used for firewood, that's it.");
   set_weight(3);
}